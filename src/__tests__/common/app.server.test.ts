describe('Scenario : Starting AppServer', () => {
  describe('Given a valid server and logger', () => {
    describe('When the AppServer starts', () => {
      test('Then it should listen on the specified port', () => {
        // Arrange
        // Act
        // Expect
      });
    });
  });
});
